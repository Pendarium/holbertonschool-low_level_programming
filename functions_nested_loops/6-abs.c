#include "main.h"
/**
* print_sign - Vérifie si un caractère est une lettre minuscule.
* @n: Le caractère à tester (en code ASCII).
*
*Return: 1 si le caractère est une lettre minuscule (entre 'a' et 'z').
*
*/
int _abs(int n)
{

if (n < 0)
return (-1);
else
return (0);
}