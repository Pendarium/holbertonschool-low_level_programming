#include <stdio.h>
/**
*main - Point d'entrée du programme
*
*Description:Cette fonction est le point de départ du programme.
*
*Return: 0 si le programme se termine correctement
*/
/**
*printf - affiche une chaîne formatée sur la sortie standard
*@format: chaîne de format contenant le texte et les spécificateurs
*
*Description:
*Cette fonction affiche une chaîne de caractères formatée
*sur la sortie standard (l’écran). Elle remplace les
*spécificateurs comme %s, %d, %c, etc. par les arguments
*correspondants.
*
*Retour : Le nombre de caractères affichés.
*/
int main(void)
{

printf("with proper grammar, but the outcome is a piece of art,\n");

return (0);
}
