#include <stdio.h>

int main(void)
{
/**
* printf - imprime texte
*
* Description: Cette fonction sert a imprimé sans retour a la ligne.
*
*/
printf( "with proper grammar, but the outcome is a piece of art,\n" );

return (0);
}
